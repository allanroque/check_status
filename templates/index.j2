<!DOCTYPE html>
<html>
<head>
    <title>System Status</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            text-align: center;
        }
        .header {
            background-color: #ddd;
            padding: 20px 0;
        }
        h1 {
            color: #333;
        }
        .url-list {
            margin: 20px auto;
            width: 80%;
            max-width: 600px;
        }
        .url {
            background-color: #fff;
            border: solid 1px #ddd;
            border-left: 5px solid;
            padding: 10px;
            margin-bottom: 10px;
            overflow: hidden;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .ok {
            border-left-color: darkgreen;
            color: darkgreen;
        }
        .notok {
            border-left-color: darkred;
            color: darkred;
        }
        .url a {
            color: inherit;
            text-decoration: none;
            flex: 1;
        }
        .status {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>SYSTEM STATUS</h1>
    </div>
    <div class="url-list">
        {% for url in urls %}
        <div class="url {{ 'ok' if url.status == 200 else 'notok' }}">
            <a href="{{ url.url }}" target="_blank">{{ url.url }}</a>
            <span class="status">{{ 'OK' if url.status == 200 else 'NOTOK' }}</span>
        </div>
        {% endfor %}
    </div>
</body>
</html>
